/**
 * Color registry for managing ANSI color codes
 */
import { ColorDefinition } from '../types';
export declare const ANSI_COLORS: Record<string, ColorDefinition>;
export declare function getColor(colorName: string): ColorDefinition | undefined;
/**
 * Parse a hex color string to RGB values
 * @param hex The hex color string (e.g., '#ff0000')
 * @returns RGB values as an array [r, g, b]
 */
export declare function parseHex(hex: string): [number, number, number];
/**
 * Convert RGB values to the closest ANSI 256 color
 * @param r Red value (0-255)
 * @param g Green value (0-255)
 * @param b Blue value (0-255)
 * @returns The ANSI 256 color code
 */
export declare function rgbToAnsi256(r: number, g: number, b: number): number;
//# sourceMappingURL=registry.d.ts.map